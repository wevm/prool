---
"prool": patch
---

Fixed Tempo instance readiness detection to support both v1.2.0 (`"Received block from consensus engine"`) and v1.3.0 (`"Received new payload from consensus engine"`) log messages.
